<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Micser.Main.Controls">

    <ControlTemplate x:Key="EmptyThumbTemplate">
        <Border Background="{TemplateBinding Background}"
                BorderThickness="{TemplateBinding BorderThickness}"
                BorderBrush="{TemplateBinding BorderBrush}" />
    </ControlTemplate>

    <Style TargetType="{x:Type controls:MoveThumb}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="SizeAll" />
        <Setter Property="Template" Value="{StaticResource EmptyThumbTemplate}" />
    </Style>

    <Style TargetType="{x:Type controls:ResizeThumb}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template" Value="{StaticResource EmptyThumbTemplate}" />
    </Style>
</ResourceDictionary>
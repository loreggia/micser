<infrastructure:View
    x:Class="Micser.Core.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Micser.Main.ViewModels"
    xmlns:infrastructure="clr-namespace:Micser.Infrastructure;assembly=Micser.Infrastructure"
    xmlns:controls="clr-namespace:Micser.Main.Controls"
    xmlns:views="clr-namespace:Micser.Main.Views"
    xmlns:widgets="clr-namespace:Micser.Main.Views.Widgets"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
    d:DesignWidth="300"
    d:DesignHeight="200"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
        </Grid>

        <controls:WidgetPanel Grid.Row="1" x:Name="WidgetPanel" WidgetFactory="{Binding WidgetFactory}" Widgets="{Binding Widgets}">
            <widgets:DeviceInputWidget Canvas.Top="50" />
            <controls:Widget></controls:Widget>
        </controls:WidgetPanel>
    </Grid>
</infrastructure:View>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:toolBars="clr-namespace:Micser.App.Infrastructure.ToolBars"
                    xmlns:converter="clr-namespace:Micser.App.Infrastructure.Converter">

    <DataTemplate x:Key="ToolBarButtonTemplate" DataType="{x:Type toolBars:ToolBarButton}">
        <Button Command="{Binding ActionCommand}">
            <StackPanel Orientation="Horizontal">
                <Image Source="{Binding IconPath}" Width="16" Height="16" Visibility="{Binding IconPath, Converter={converter:EmptyStringToVisibilityConverter}}" />
                <TextBlock Text="{Binding Name}" Height="16" Visibility="{Binding Name, Converter={converter:EmptyStringToVisibilityConverter}}" />
            </StackPanel>
        </Button>
    </DataTemplate>
    <DataTemplate x:Key="ToolBarLabelTemplate" DataType="{x:Type toolBars:ToolBarLabel}">
        <TextBlock Text="{Binding Name}" ToolTip="{Binding Description}" />
    </DataTemplate>

    <toolBars:ToolBarItemTemplateSelector
        x:Key="ToolBarItemTemplateSelector"
        ButtonTemplate="{StaticResource ToolBarButtonTemplate}"
        LabelTemplate="{StaticResource ToolBarLabelTemplate}" />

    <Style TargetType="{x:Type ToolBar}">
        <Setter Property="ItemTemplateSelector" Value="{StaticResource ToolBarItemTemplateSelector}" />
    </Style>
</ResourceDictionary>
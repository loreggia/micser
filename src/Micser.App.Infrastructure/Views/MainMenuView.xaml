<i:View
    x:Class="Micser.App.Infrastructure.Views.MainMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:Micser.App.Infrastructure"
    xmlns:viewModels="clr-namespace:Micser.App.Infrastructure.ViewModels"
    mc:Ignorable="d"
    d:DesignWidth="300"
    d:DataContext="{d:DesignInstance viewModels:MainMenuViewModel}">
    <Grid>
        <Menu ItemsSource="{Binding MenuItems}" IsMainMenu="True">
            <Menu.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                    <HierarchicalDataTemplate.ItemContainerStyle>
                        <Style TargetType="{x:Type MenuItem}">
                            <Setter Property="Command" Value="{Binding Item.Command}" />
                        </Style>
                    </HierarchicalDataTemplate.ItemContainerStyle>
                    <ContentPresenter Content="{Binding Item.Header}" RecognizesAccessKey="True" />
                </HierarchicalDataTemplate>
            </Menu.ItemTemplate>
        </Menu>
    </Grid>
</i:View>
<i:View x:Class="Micser.App.Views.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="clr-namespace:Micser.App.Infrastructure;assembly=Micser.App.Infrastructure"
        xmlns:vm="clr-namespace:Micser.App.ViewModels"
        xmlns:themes="clr-namespace:Micser.App.Infrastructure.Themes;assembly=Micser.App.Infrastructure"
        mc:Ignorable="d"
        d:DesignHeight="450"
        d:DesignWidth="800"
        d:DataContext="{d:DesignInstance vm:AboutViewModel}">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="/icon.ico" VerticalAlignment="Top" HorizontalAlignment="Center" Stretch="None" />
            <TextBlock Text="{Binding VersionText}" HorizontalAlignment="Center" Foreground="{DynamicResource {themes:BrushThemeKey DefaultForeground}}" />
            <TextBlock Text="{Binding AboutText}" HorizontalAlignment="Center" Foreground="{DynamicResource {themes:BrushThemeKey DefaultForeground}}" />
        </StackPanel>
    </Grid>
</i:View>
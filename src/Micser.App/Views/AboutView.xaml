<i:View x:Class="Micser.App.Views.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="clr-namespace:Micser.App.Infrastructure;assembly=Micser.App.Infrastructure"
        xmlns:vm="clr-namespace:Micser.App.ViewModels"
        mc:Ignorable="d"
        d:DesignHeight="450"
        d:DesignWidth="800"
        d:DataContext="{d:DesignInstance vm:AboutViewModel}">
    <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxWidth="800">
        <StackPanel Orientation="Vertical">
            <ContentControl ContentTemplate="{DynamicResource Logo}" Margin="50" MinWidth="64" MaxWidth="512" />
            <TextBlock Text="{Binding VersionText}" HorizontalAlignment="Center" />
            <TextBlock Text="{Binding AboutText}" HorizontalAlignment="Center" />
        </StackPanel>
    </ScrollViewer>
</i:View>
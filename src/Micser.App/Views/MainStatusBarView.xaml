<i:View x:Class="Micser.App.Views.MainStatusBarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="clr-namespace:Micser.App.Infrastructure;assembly=Micser.App.Infrastructure"
        xmlns:vm="clr-namespace:Micser.App.ViewModels"
        xmlns:themes="clr-namespace:Micser.App.Infrastructure.Themes;assembly=Micser.App.Infrastructure"
        mc:Ignorable="d"
        d:DesignWidth="300"
        d:DataContext="{d:DesignInstance vm:MainStatusBarViewModel}">
    <Grid>
        <StatusBar Background="{DynamicResource {themes:BrushThemeKey StatusBarBackground}}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource {themes:BrushThemeKey DefaultForeground}}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</i:View>
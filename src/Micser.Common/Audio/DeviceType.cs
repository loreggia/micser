namespace Micser.Common.Audio
{
    /// <summary>
    /// The type of an audio device.
    /// </summary>
    public enum DeviceType
    {
        /// <summary>
        /// An input/capture device (i.e. microphone).
        /// </summary>
        Input,

        /// <summary>
        /// An output/render device (i.e. speakers).
        /// </summary>
        Output
    }
}